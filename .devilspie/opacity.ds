( if
( or
( contains ( window_class ) "URxvt" )
)
( begin
( spawn_async (str "transset -i " (window_xid) " 0.75" ))
)
)

( if
( or
( contains ( window_class ) "Code" )
)
( begin
( spawn_async (str "transset -i " (window_xid) " 0.85" ))
)
)

( if
( or
( contains ( window_class ) "obsidian" )
)
( begin
( spawn_async (str "transset -i " (window_xid) " 0.87" ))
)
)
